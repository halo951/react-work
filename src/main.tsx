import { createRouter } from "./library/router";

// 1. 创建渲染容器
const el = document.createElement("div");
document.body.appendChild(el);

// 2. 创建路由
const router = createRouter([]);
// 3. 创建渲染器

// 4. 渲染
